<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<link rel="stylesheet" type="text/css" href="css/styles.css">
	<link href='https://fonts.googleapis.com/css?family=Special+Elite' rel='stylesheet' type='text/css'>

	<title>Blog App</title>
</head>

<body>
	<div class="container">
		<div>
			<p>

			</p>
		</div>
		<p>Radical innovative balderdash</p>

		<h1>First blog app</h1>
		<div class="nav">
			<% Sinatra::Application.each_route do |route| %>
			<% if route.verb == "GET" %>

			<%= li_with_a :class => "", :url => route.path, :text => route.path %>
			<% end %>
		<% end %>
		</div>

		<%= yield_content :index %>

			<%= yield_content :blog %>

				<%= yield_content :new_entry %>

	</div>
	<div>
		<%= yield %>
	</div>






</body>

</html>
